import { Patient } from '../types';

// Pacjenci będą generowani i przypisywani do specjalistów w sampleData.ts
export const samplePatients: Patient[] = [
  // Przykładowi pacjenci, do uzupełnienia przez generator w sampleData.ts
];
